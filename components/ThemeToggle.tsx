import React from 'react';
import { View } from 'react-native';

// Theme toggle is disabled - always use dark theme with Heinicus Designs brand colors
export function ThemeToggle() {
  return <View />;
}